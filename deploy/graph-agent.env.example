# Secrets for graph-agent. Installed as /etc/graph-agent/env (root:root, 0600).
# Non-secret settings belong in config.yaml / config.local.yaml.
# load_dotenv() does NOT override variables already set here.

LITELLM_API_KEY=
# TELEGRAM_BOT_TOKEN=
# Bearer token for the HTTP transport, referenced from channels.http.api_key_env
# GRAPH_AGENT_HTTP_TOKEN=